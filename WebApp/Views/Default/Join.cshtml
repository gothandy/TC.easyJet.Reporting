<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Join</title>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Month</th>
                <th>Epic</th>
                <th>DomId</th>
                <th>List</th>
                <th>Name</th>
                <th>UserName</th>
                <th>Billable</th>
                <th>Invoice</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Vincente.WebApp.Models.JoinModel join in Model)
            {
                <tr>
                    <td>@String.Format("{0:d}", join.Month)</td>
                    <td>@join.Epic</td>
                    <td>@join.DomId</td>
                    <td>@join.List</td>
                    <td>@join.Name</td>
                    <td>@join.UserName</td>
                    <td>@string.Format("{0:C0}", join.Billable)</td>
                    <td>@string.Format("{0:d}", join.Invoice)</td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
