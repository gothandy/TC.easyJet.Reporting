@model IEnumerable<JoinModel>
<h2>Data</h2>
<h3>All By Month</h3>
<table>
    <thead>
        <tr>
            <th>Month</th>
            <th>Epic</th>
            <th>DomId</th>
            <th>Index</th>
            <th>List</th>
            <th>Name</th>
            <th>UserName</th>
            <th>Billable</th>
            <th>Invoice</th>
        </tr>
    </thead>
    <tbody>
        @foreach (JoinModel item in Model)
        {
            <tr>
                <td>@string.Format("{0:d}", item.Month)</td>
                <td>@item.Epic</td>
                <td>@item.DomId</td>
                <td class="number">@item.ListIndex</td>
                <td>@item.ListName</td>
                <td>@item.Name</td>
                <td>@item.UserName</td>
                <td class="currency">@string.Format("{0:n2}", item.Billable)</td>
                <td>@string.Format("{0:d}", item.Invoice)</td>
            </tr>
        }
    </tbody>
</table>
