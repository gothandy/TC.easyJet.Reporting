@model IEnumerable<Task>

<ol class="breadcrumb">
    <li><a href="/">Home</a></li>
    <li><a href="/Data">Data</a></li>
    <li class="active">Tasks</li>
</ol>

<div class="page-header">
    <h2>Tasks</h2>
</div>

<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>Project Id</th>
                <th>Id</th>
                <th>Name</th>
                <th>Project Name</th>
                <th>Active</th>
                <th>Tracked Seconds</th>
                <th>Card Id</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Task item in Model)
            {
                <tr>
                    <td>@item.ProjectId</td>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.ProjectName</td>
                    <td>@item.Active</td>
                    <td>@item.TrackedSeconds</td>
                    <td>@item.CardId</td>
                </tr>
            }
        </tbody>
    </table>
</div>